extends layout

block content
  h1= title
  ul
    - each recipe in recipes
      li
        span(class=recipe.type) #{recipe.type}
        span
          a(href='recipes/'+recipe.label) #{recipe.label}
